<span class="brand-mark-provisional" data-brand-status="provisional" aria-hidden="true">
  <span>RD</span>
</span>

<style>
  .brand-mark-provisional {
    position: relative;
    width: 3rem;
    height: 3rem;
    flex: 0 0 auto;
    display: grid;
    place-items: center;
    overflow: hidden;
    border-radius: var(--radius-md);
    color: var(--color-text-inverse);
    background: var(--color-editorial-black);
    box-shadow: inset 0 0 0 1px rgb(var(--color-text-inverse-rgb) / 0.14);
  }

  .brand-mark-provisional::before,
  .brand-mark-provisional::after {
    content: '';
    position: absolute;
    left: 52%;
    border: 1px solid var(--color-signal);
    border-radius: 999px;
    opacity: 0.68;
  }

  .brand-mark-provisional::before {
    top: -8%;
    width: 3.35rem;
    height: 3.35rem;
  }

  .brand-mark-provisional::after {
    top: 20%;
    width: 2rem;
    height: 2rem;
  }

  .brand-mark-provisional span {
    position: relative;
    z-index: 1;
    transform: translateX(-0.1rem);
    font-size: 0.9rem;
    font-weight: 900;
    letter-spacing: -0.08em;
  }

  @media (max-width: 63.999rem) {
    .brand-mark-provisional {
      width: 2.5rem;
      height: 2.5rem;
      border-radius: 0.75rem;
    }

    .brand-mark-provisional::before {
      width: 2.8rem;
      height: 2.8rem;
    }

    .brand-mark-provisional::after {
      width: 1.65rem;
      height: 1.65rem;
    }
  }

  @media (max-width: 39rem) {
    .brand-mark-provisional {
      width: 2.25rem;
      height: 2.25rem;
      border-radius: 0.65rem;
    }
  }
</style>
