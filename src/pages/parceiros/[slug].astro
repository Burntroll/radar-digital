---
import { getCollection } from 'astro:content';

// ═══════════════════════════════════════════════════════════
// REDIRECT FALLBACK (local dev)
// /parceiros/[slug]/ → /ferramentas/[slug]/
// Em producao, o vercel.json faz o redirect 308 real.
// ═══════════════════════════════════════════════════════════

export async function getStaticPaths() {
  const categorias = await getCollection('parceiroCategorias', ({ data }) => data.draft === false);
  return categorias.map((cat) => ({
    params: { slug: cat.slug },
  }));
}

const { slug } = Astro.params;
---
<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8" />
  <meta name="robots" content="noindex, nofollow" />
  <meta http-equiv="refresh" content={`0; url=/ferramentas/${slug}/`} />
</head>
<body>
  <p>Redirecionando para <a href={`/ferramentas/${slug}/`}>/ferramentas/{slug}/</a>...</p>
</body>
</html>
